allprojects{
    group = 'cn.yerl.web'
    version = '1.0.1.SNAPSHOT'
    ext {
        JDK_VERSION = 1.8
        STARTER_VERSION = '1.5.7.RELEASE'
    }
}

subprojects{
    buildscript {
        repositories {
            mavenLocal()
            maven { url 'http://maven.minstone.com.cn/content/groups/public/'}
            maven { url 'https://oss.sonatype.org/content/groups/public/'}
            mavenCentral()
            jcenter()
        }
        dependencies {
            classpath 'cn.yerl.gradle:nexus-plugin:+'
        }
    }
    repositories {
        mavenLocal()
        maven { url 'http://maven.minstone.com.cn/content/groups/public/'}
        mavenCentral()
        jcenter()
    }
}